import React from 'react';
import './LoadingSpinner.css';  // Link to the CSS

const LoadingSpinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default LoadingSpinner;
